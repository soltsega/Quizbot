# Telegram QuizBot

A fun and engaging quiz experience for Telegram groups, built from scratch.

## Setup

1. Clone the repository.
2. Install dependencies: `npm install`
3. Create a `.env` file and add your `BOT_TOKEN`.
4. Run the bot: `npm start`
